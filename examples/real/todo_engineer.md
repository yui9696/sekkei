TODO for the new service (from Kenji)

* replace the cron job that emails the CSV
* API for the mobile team: list devices, device detail, update label
* auth via existing JWT (same as the other services)
* ~15k devices, mobile polls every 60s -> keep it cheap
* Postgres, Go, k8s. just me for now, Maya joins next month
* don't break the old CSV email until the mobile app ships
