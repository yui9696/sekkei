@channel quick one before I go on leave — we need the internal "flaky test tracker" built this sprint. basically: CI (GitHub Actions) posts every test result to us, we keep the last 30 days per test, and flag anything that flipped pass/fail on the same commit more than 3 times as flaky. devs get a slack DM when a test they own goes flaky, and there's a page listing flaky tests per repo sorted by flip count. we have ~200 repos, ~50k test cases, roughly 2M results/day. store in postgres (already have it), one python service, team is me + Lena. nothing fancy, no auth beyond github SSO which we already have. must not slow CI down — the results post should be fire and forget, <100ms. also please make the quarantine list exportable as JSON so the CI can skip quarantined tests. thx
